# ServerClientApplication
File Transmission Application
Authenticated with SSL certification.
Server contains SSL certificate.
Client uses trustStore to validate certificate.
Program uses one-way handshaking, server can handle multiple requests at a time.
